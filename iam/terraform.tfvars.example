# Example configuration for ECS IAM module
# Copy this to terraform.tfvars and update values as needed

app_name              = "docker-hands-on"
environment           = "dev"
aws_region            = "us-east-1"
enable_secrets_access = true
